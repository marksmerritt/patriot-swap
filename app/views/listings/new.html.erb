<%= form_with model: @listing do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, id: "title-field" %>

  <%= f.label :price_cents %>
  <%= f.text_field :price_cents, id: "price-cents-field" %>

  <%= f.submit "Post", id: "listing-submit-btn" %>
<% end %>